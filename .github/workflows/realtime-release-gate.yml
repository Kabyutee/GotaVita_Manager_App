name: Realtime Release Gate

on:
  push:
    branches:
      - release/realtime-sync-verified

permissions:
  contents: read

jobs:
  classify-and-test:
    name: Classify change and run minimal safe gate
    runs-on: ubuntu-latest
    steps:
      - name: Checkout release commit
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: JARVIS 9.0 business-first adaptive workflow
        run: node tests/jarvis-9-business-first-adaptive-workflow.test.js

      - name: JARVIS 7.0 adaptive whole-application scan
        run: node tests/anti-big-bang-7-adaptive-application-scan.test.js

      - name: JARVIS 8.0 runtime adaptation audit
        run: node tests/jarvis-8-runtime-adaptation-audit.test.js

      - name: JARVIS whole-app hardening contract
        run: node tests/jarvis-whole-app-hardening-contract.test.js

      - name: Realtime sync architecture contracts
        run: |
          node tests/sprint18-transaction-sync-readiness-contract.test.js
          node tests/sprint18-sync-queue-preservation-contract.test.js
          node tests/sprint20-sync-transaction-no-requeue-contract.test.js
          node tests/sprint21-whole-sync-hardening-contract.test.js
          node tests/sprint22-complete-runtime-sync-repair.test.js
          node tests/anti-big-bang-5-sync-architecture-contract.test.js
          node tests/anti-big-bang-6-application-connection-audit.test.js
          node tests/sprint20-empty-resource-convergence-contract.test.js

      - name: Syntax checks
        run: |
          node --check js/core/order-write-boundary-bridge.js
          node --check js/core/data-gateway.js
          node --check js/core/sync-manager.js
          node --check worker.js

      - name: Publish release gate result
        run: echo "Realtime release gate passed for $GITHUB_SHA"
